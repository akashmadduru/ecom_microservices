name: auth-service CI

on:
  pull_request:
    paths:
      - 'python/services/auth_service/**'
      - 'python/libs/ecom_common/**'
      - 'python/pyproject.toml'
      - 'deploy/docker/entrypoint.sh'
      - '.github/workflows/ci-auth-service.yml'
      - '.github/workflows/_reusable-python-lint-test.yml'
      - '.github/workflows/_reusable-docker-build-push.yml'
  push:
    branches: [main]
    paths:
      - 'python/services/auth_service/**'
      - 'python/libs/ecom_common/**'
      - 'python/pyproject.toml'
      - 'deploy/docker/entrypoint.sh'
      - '.github/workflows/ci-auth-service.yml'
      - '.github/workflows/_reusable-python-lint-test.yml'
      - '.github/workflows/_reusable-docker-build-push.yml'

concurrency:
  group: ci-auth-service-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-test:
    uses: ./.github/workflows/_reusable-python-lint-test.yml
    permissions:
      contents: read
    with:
      working-path: services/auth_service
      package-name: auth-service
      run-integration-tests: false

  docker:
    needs: [lint-test]
    uses: ./.github/workflows/_reusable-docker-build-push.yml
    permissions:
      contents: read
      packages: write
    with:
      context: .
      dockerfile-path: python/services/auth_service/Dockerfile
      image-name: ghcr.io/${{ github.repository_owner }}/ecom-microservices-auth-service
      push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
